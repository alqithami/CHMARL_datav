name: Apply PocketWorld snapshot offset pagination

on:
  push:
    branches:
      - agent/complete-pocketworld-snapshot-offset
    paths:
      - scripts/apply-pocketworld-snapshot-offset.mjs
      - .github/workflows/apply-pocketworld-snapshot-offset.yml

permissions:
  contents: write

concurrency:
  group: apply-pocketworld-snapshot-offset
  cancel-in-progress: true

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out pagination branch
        uses: actions/checkout@v6
        with:
          ref: agent/complete-pocketworld-snapshot-offset
          fetch-depth: 0
          persist-credentials: true

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: .node-version
          package-manager-cache: false

      - name: Enable pinned pnpm
        run: |
          corepack enable
          corepack prepare pnpm@9.15.9 --activate
          pnpm --version

      - name: Apply reviewed snapshot pagination change
        run: |
          node --check scripts/apply-pocketworld-snapshot-offset.mjs
          node scripts/apply-pocketworld-snapshot-offset.mjs
          node --check server/vessel-feed-proxy/pocketworld-live-ais.mjs

      - name: Install locked dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint, build, and verify runtime
        run: pnpm check

      - name: Verify generated artifacts
        run: pnpm verify:dist

      - name: Check repository diff
        run: git diff --check

      - name: Build production container
        run: docker build --tag chmarl-datav:snapshot-offset .

      - name: Smoke production container
        shell: bash
        run: |
          set -euo pipefail
          docker run --detach --name chmarl-snapshot-offset --publish 8787:8787 chmarl-datav:snapshot-offset
          cleanup() {
            docker logs chmarl-snapshot-offset || true
            docker rm --force chmarl-snapshot-offset >/dev/null 2>&1 || true
          }
          trap cleanup EXIT

          for attempt in $(seq 1 30); do
            if curl --fail --silent --show-error http://127.0.0.1:8787/health/live > /tmp/chmarl-live.json; then
              break
            fi
            if [[ "$attempt" == "30" ]]; then
              echo "The production container did not become live in time." >&2
              exit 1
            fi
            sleep 2
          done

          cat /tmp/chmarl-live.json
          curl --fail --silent --show-error http://127.0.0.1:8787/version

      - name: Commit validated snapshot pagination
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No generated changes to commit."
            exit 0
          fi
          git commit -m "Complete PocketWorld truncated snapshots"
          git push origin HEAD:agent/complete-pocketworld-snapshot-offset
